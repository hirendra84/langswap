# qwen-asr 0.0.6 hard-pins transformers==4.57.6, accelerate==1.12.0,
# nagisa==0.2.11, etc.  Installed WITHOUT --no-deps so all of those land
# at the versions qwen-asr was built against — no transformers-5.x shims
# needed, the loader Just Works inside this isolated container.
qwen-asr==0.0.6

# HTTP service
fastapi
uvicorn[standard]
python-multipart

# Audio I/O
soundfile
