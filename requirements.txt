# Core ML
torch
torchaudio
torchcodec  # required by torchaudio 2.10+ for audio save
numpy
transformers
huggingface_hub>=0.20.0

# ASR (qwen backend)
# qwen-asr is installed separately with --no-deps because it hard-pins
# transformers==4.57.6, which conflicts with omnivoice (needs transformers>=5.x).
# Its code works fine on transformers 5.x; the pin is just upstream conservatism.
# See Dockerfile / Makefile for the two-step install.
# qwen-asr   ← installed via: uv pip install qwen-asr --no-deps

# vLLM (translation + TTS backend)
vllm
vllm-omni
# qwen-tts hard-pins transformers==4.57.3, installed separately with --no-deps (optional/legacy)
# qwen-tts   ← uv pip install qwen-tts --no-deps
soundfile

# Audio processing
pydub
demucs @ git+https://github.com/shigabeev/demucs@9e454051dc5ed957b13525eaad82a2e74e8e5dcb
pyrubberband
silero-vad

# Data / serialization
pandas
attrs
cattrs
pydantic

# Utils
tqdm
requests
python-dotenv
boto3

# API clients (Mac-friendly, no local GPU)
openai

# Local demo UI
gradio>=4.0.0
